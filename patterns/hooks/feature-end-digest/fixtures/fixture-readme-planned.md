# F90 — Foo feature

Status: planned

## Goal

Hermetic test fixture representing an *in-progress* feature: the digest hook
must NOT drop anything because the feature has not shipped (no `Status: done`).
