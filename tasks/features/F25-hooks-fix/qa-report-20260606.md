# QA Report — F25 hooks-fix

**Date:** 2026-06-06
**Verdict:** PASSED

---

## Per-Criterion Pass/Fail Table

| AC | Criterion | Status | Evidence |
|---|---|---|---|
| AC1 | `grep -r '{{' */.claude/settings.json` + template → 0 | ✅ PASS | `bin/tests/F25-unit.sh`: 9 files × 0 `{{` |
| AC2 | `bash ${DIDIO_HOME:-...}/bin/hooks/didio-pre-tool.sh` without "No such file" | ✅ PASS | Unit test assertion #28, exit 0 |
| AC3 | No generic downstream contains `{{DIDIO_SECOND_BRAIN_HOME}}` hooks | ✅ PASS | Unit test: 6 generic projects × PASS |
| AC4 | `--dry-run` on clean project: 0 hooks with `{{` | ✅ PASS | Integration test: 9/9 |
| AC5 | 7 downstream + upstream committed; rollback tag preserved | ✅ PASS | All 7 downstream: commits + `pre-didio-sync-20260605` tag; upstream committed (4349e36) |

### Per-Task AC Detail

**T01** (template hooks fix):
- `grep -c '{{' templates/.claude/settings.json` → 0 ✅
- No `{{DIDIO_SECOND_BRAIN_HOME}}` hook ✅
- 2 framework hooks use `${DIDIO_HOME:-$HOME/.claude-didio-config}` ✅
- JSON valid ✅

**T02** (upstream sanitization):
- `grep -c '{{' .claude/settings.json` → 0 ✅
- No duplicate commands ✅ (13 hooks → 3 clean hooks)
- No hardcoded `/Users/eduardodidio/didio-second-brain-claude` in hooks scope ✅
- `permissions` preserved; JSON valid ✅
- Note: upstream uses `$CLAUDE_PROJECT_DIR` instead of `${DIDIO_HOME:-...}` — intentional per developer note, functionally equivalent for dogfood repo

**T03** (sync placeholder-safety):
- `{{` in source → hook skipped + `skipped_ph` counter reported ✅
- `--dry-run` on clean project: 0 `{{` hooks ✅
- Valid hooks (no `{{`) continue merging normally ✅
- Boundary (no pre-existing hooks block): only valid hooks created ✅

**T04** (7 downstream cleanup):
- All 7 downstreams: 0 `{{`, JSON valid ✅
- 6 generic projects: no `DIDIO_SECOND_BRAIN_HOME` hooks ✅
- `didio-second-brain-claude`: second-brain hooks resolved to real paths ✅
- All 7: framework hooks use `${DIDIO_HOME:-...}` ✅
- `didio-second-brain-claude`: no duplicate hooks (B1 follow-up resolved) ✅
- `didio-second-brain-clean-starter`: no hook accumulation (B2 follow-up resolved) ✅

---

## TechLead Follow-up Items — Disposition

| Item | Disposition |
|---|---|
| **B1** Fix duplicates in `didio-second-brain-claude` | ✅ FIXED — commit `6d2fd6b` |
| **B2** Fix `didio-second-brain-clean-starter` hook accumulation | ✅ FIXED — commit `31d6b9b` |
| **B3** Create `bin/tests/F25-unit.sh` | ✅ CREATED by QA — 28 assertions, all PASS |
| Future task (pre-existing hardcoded paths) | ⏭ Deferred — separate feature per TechLead note |

---

## New Tests Added

Created `bin/tests/F25-unit.sh` (was missing, listed in F25-test-plan.md as required harness):

**Assertions (28 total):**
- JSON validity: template + upstream + 7 downstream (9 checks)
- No `{{`: template + upstream + 7 downstream (9 checks)
- Template uses `${DIDIO_HOME:-...}` convention (2 checks)
- No `DIDIO_SECOND_BRAIN_HOME` in 6 generic downstreams (6 checks)
- Shell fallback expansion: `DIDIO_HOME=` unset → `$HOME/.claude-didio-config` (1 check)
- `didio-pre-tool.sh` executes without ENOENT (1 check)

---

## Test Command Output Summary

```
bin/tests/F25-unit.sh:
Results: 28 passed, 0 failed

bin/tests/F25-integration.sh:
Results: 9 passed, 0 failed
```

---

## Blockers

None. All TechLead follow-up items resolved.

---

## Upstream Commit

AC5 required upstream commit. Files staged and committed as `4349e36`:
- `templates/.claude/settings.json` — T01 changes
- `.claude/settings.json` — T02 changes
- `bin/didio-sync-project.sh` — T03 changes
- `bin/tests/F25-unit.sh` — new unit harness (QA-created)

---

## Final Verdict: PASSED
