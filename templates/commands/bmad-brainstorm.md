---
description: "[alias bmad] Gera 3–5 direções de design com trade-offs — delega a /brainstorm"
argument-hint: "<topic>"
---

<!-- bmad-alias: delegates-to=/brainstorm persona=PB -->

# /bmad-brainstorm — alias de /brainstorm

Este é um **alias fino** do modo BMAD. Ele delega integralmente ao comando
`/brainstorm` do didio. Persona BMAD: **PB**.

Execute exatamente o que `/brainstorm $ARGUMENTS` faria — não reimplemente a
lógica. Carregue e siga `templates/commands/brainstorm.md` com os mesmos
argumentos.
